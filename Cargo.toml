[workspace]
resolver = "2"
members = [
    "crates/sld-core",
    "crates/sld-sources/forza",
    "crates/sld-outputs/logitech-hid",
    "crates/sld-outputs/aux-display",
    "crates/sld-service",
    "crates/sld-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["Glenn Schuurman"]

# Centralized so every crate in the workspace stays on the same version of
# shared dependencies. Crate-local Cargo.toml files reference these via
# `{ workspace = true }` and only add crate-specific deps of their own.
[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
async-trait = "0.1"
